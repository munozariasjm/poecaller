from setuptools import setup, find_packages

setup(
    name="poe_caller",
    version="0.0.1",
    packages=find_packages(),
    description="nada",
    author="munozariasjm",
    install_requires=["poe-api"],
)
